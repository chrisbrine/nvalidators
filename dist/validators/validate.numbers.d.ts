import { Validators } from "../types";
export declare function minNumber(param: string | number, min: number): boolean;
export declare function maxNumber(param: string | number, max: number): boolean;
export declare const validators: Validators;
